# Standalone Prototype

This is a standalone export of a BOI Prototype instance. It runs as its own Next.js app.

## Run locally

```bash
npm install
npm run dev
```

Open [http://localhost:3000](http://localhost:3000). The instance data is in `public/instance.json`.

## Build for production

```bash
npm run build
npm start
```
